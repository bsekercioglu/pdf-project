# ===========================================
# PDF İşleme Merkezi - Ortam Değişkenleri
# Kopyalayın: cp .env.example .env
# ===========================================

# Çalışma portu (Docker'da da kullanılır)
PORT=3005

# Veritabanı (Prisma - SQLite)
DATABASE_URL="file:./dev.db"

# JWT - Giriş oturumları için (üretimde mutlaka güçlü bir değer kullanın)
JWT_SECRET=your-super-secret-key-change-in-production

# Giriş cookie - Sunucuda giriş tutmuyorsa (HTTPS proxy arkasında) ekleyin:
# SECURE_COOKIE=false

# Dosya klasörleri (opsiyonel; varsayılan: proje kökü altında)
# UPLOAD_FOLDER=uploads
# OUTPUT_FOLDER=outputs
# TEMP_FOLDER=temp_files

# Üretim ortamı (next build / next start ile otomatik production)
# NODE_ENV=production
